# products/

One folder per weekly product, named `NNN-slug` (e.g. `001-reddit-gap-finder`), each fully self-contained.

To start a new one: copy `../templates/web-app`, then follow [docs/conventions.md](../docs/conventions.md).
